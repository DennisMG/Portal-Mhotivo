@model IEnumerable<Mhotivo.Models.NotificationCommentDisplayModel>
<link href="~/Content/PagedList.css" rel="stylesheet" type="text/css" />
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<a class="toEdit btn btn-small btn-primary" style="float: right" data-toggle="modal" role="button" data-target="#EditModal" id="/NotificationComment/Add/@ViewBag.NotificationId">Agregar comentario</a>
<h2>Comentarios</h2>
@if (!Model.Any())
{
    <div class="well well-large">
        <h4 class="blue">Tabla Vacía</h4>
        No hay comentarios a mostrar.
    </div>
}
else
{
    <table class="table table-striped table-bordered table-hover" id="sample-table-1">
        <thead>
        <tr>
            <th>@Html.DisplayNameFor(model => model.Commenter)</th>
            <th>@Html.DisplayNameFor(model => model.CommentText)</th>
            <th>@Html.DisplayNameFor(model => model.CreationDate)</th>
            <th>Borrar</th>
        </tr>
        </thead>
        <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@Html.DisplayFor(modelItem => item.Commenter)</td>
                <td>@Html.DisplayFor(modelItem => item.CommentText)</td>
                <td>@Html.DisplayFor(modelItem => item.CreationDate)</td>
                <td>
                    @Html.ActionLink("Borrar Comentario", "Delete", "NotificationComment",
                        new {notificationId = ViewBag.NotificationId, commentId = Html.DisplayFor(modelItem => item.Id)}, null)
                </td>
            </tr>
        }
        </tbody>
    </table>
}
<div tabindex="-1" class="modal hide fade" id="EditModal" style="display: none;" aria-hidden="true">
    <div id="body" class="modal-body">

    </div>
</div>